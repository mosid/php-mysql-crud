apiVersion: v1
kind: Secret
metadata:
  name: php-crud-secret
  labels:
    app: php-crud
type: Opaque
# These are placeholder values. In production, use:
#   - External Secrets Operator with AWS Secrets Manager
#   - Sealed Secrets
#   - Vault
# NEVER commit real credentials. This file is a template only.
stringData:
  DB_PASSWORD: "REPLACE_WITH_REAL_SECRET"
